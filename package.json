{
  "name": "as-web-sitesi",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "yayinla": "git add -A && git commit -m \"Site güncellemesi\" ; git push",
    "yayinla:cloudflare": "astro build && wrangler pages deploy dist --project-name akarca-sahanturk --branch main --commit-dirty=true"
  },
  "dependencies": {
    "astro": "^5.6.1"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
